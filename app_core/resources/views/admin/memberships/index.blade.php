@extends('layouts.admin') @section('content')<section class="section"><h1>Membership Plans</h1><form method="post">@csrf<input name="name" placeholder="Plan"><input name="price" placeholder="Price"><input name="duration_days" placeholder="Days"><input name="ad_limit" placeholder="Ad limit"><input name="product_limit" placeholder="Product limit"><button>Add Plan</button></form>@foreach($plans as $p)<div class="row"><b>{{$p->name}}</b><span>{{$p->price}}</span><span>{{$p->duration_days}} days</span></div>@endforeach</section>@endsection