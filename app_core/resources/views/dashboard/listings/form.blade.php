@extends('layouts.app') @section('content')<section class="form-page"><form method="post" action="/dashboard/listings">@csrf<h1>Post Listing</h1><select name="type"><option value="classified">Classified Ad</option><option value="product">Store Product</option></select><select name="store_id"><option value="">No store / personal ad</option>@foreach($stores as $s)<option value="{{$s->id}}">{{$s->name}}</option>@endforeach</select><select name="category_id">@foreach($categories as $c)<option value="{{$c->id}}">{{$c->name}}</option>@endforeach</select><input name="title" placeholder="Title"><input name="price" placeholder="Price"><input name="location" placeholder="Location"><textarea name="description" placeholder="Description"></textarea><button class="btn">Submit for Approval</button></form></section>@endsection